require "bundler"
Bundler.setup

require "rake"
#require "rspec"
#require "rspec/core/rake_task"

#RSpec::Core::RakeTask.new("spec") do |spec|
#  spec.pattern = "spec/**/*_spec.rb"
#end

#task :default => :spec

task :hello => do
	puts "hello, Lisa! I am the first rake task you ever made! :)"
puts "the sweet and simple joys of life"
end